#include <stdio.h>

/**
 * main -entry point
 *
 *Return: 0 Success
 */

int main(void)
{
	char kenyor;

	for (kenyor = 'a'; kenyor = 'z'; kenyor++)
	{
		if (kenyor != q && kenyor != e)
			putchar(kenyor);
	}
	putchar('\n';)
	return (0);
}
